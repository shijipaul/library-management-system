server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shijipaul/library-config-repo.git
          clone-on-start: true

  application:
    name: config-server